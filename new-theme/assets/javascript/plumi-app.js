(function() {

    var plumiApp = angular.module('plumi_app', ['plumi_home_app'] );

    /* TODO: Enable on production version */
    /*mainApp.config(['$compileProvider', function ($compileProvider) {
      $compileProvider.debugInfoEnabled(false);
    }]);*/

	(function($) {
		
	}(jQuery));

})();